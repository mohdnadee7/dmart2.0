import { Link } from "expo-router";
import { View,Text, TouchableOpacity, StyleSheet } from "react-native";

const TopHeader=()=>{
    return (
        <View style={styles.outer}>
           
        </View>
    );
}
const styles= StyleSheet.create({
    outer:{
    backgroundColor:"#018786",height:40
},

});
export default TopHeader